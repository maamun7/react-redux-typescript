import { defineMessages } from 'react-intl';

export const scope = 'project.pages.LocaleSwitcher';

export default defineMessages({
	en: {
		id: `${scope}.en`,
		defaultMessage: 'en'
	},
	bn: {
		id: `${scope}.bn`,
		defaultMessage: 'bn'
	}
});
